<header>
  {{#if loggedIn}}
  {{> private-navbar}}
  {{else}}
  {{> public-navbar}}
  {{/if}}
</header>

<h1 class="m-3">Latest Blogs</h1>

<main id="container">
    {{#if loggedIn}}
    {{#each blogData}}
     {{> blogs-private}}
  {{/each}}
  {{else}}
  {{#each blogData}}
     {{> blogs-public}}
  {{/each}}
   {{/if}}
</main>