<header>
  {{#if loggedIn}}
  {{> private-navbar}}
  {{else}}
  {{> public-navbar}}
  {{/if}}
</header>

<main>
<div class="d-flex justify-content-center">
<h1 class="m-3">Care to comment?</h1>
</div>
  {{#if loggedIn}}
{{#with blogData}}
     {{> comment-blog}}
{{/with}}
  {{else}}
{{#with blogData}}
{{>blogs-public}}
{{/with}}
{{/if}}
</main>